# ProGress UI — Hugging Face ZeroGPU runtime (Python 3.10).
#
# ZeroGPU requires torch in {2.8.0, 2.9.1, 2.10.0, 2.11.0}; we use 2.8.0 (the
# lowest, for best compatibility with the rest of the stack).  `spaces` provides
# the @spaces.GPU decorator that attaches a GPU during generation, and the app
# falls back to CPU automatically if the GPU is unavailable.
#
# The checkpoint is re-saved as an inference-only slim checkpoint so it loads
# cleanly under Lightning 2.6 (the original embedded Lightning-2.0 Trainer state).
# torch-scatter/sparse companion wheels are NOT needed (PyG pure-PyTorch fallback);
# torchvision is NOT imported by the inference path, so it is omitted.
spaces

torch==2.8.0
torch_geometric==2.6.1
pytorch-lightning==2.6.5
torchmetrics==1.9.0

hydra-core==1.3.2
omegaconf==2.3.0

numpy<2
scipy
pandas
scikit-learn
networkx
matplotlib
imageio

music21==8.3.0
rdkit
wandb

gradio==6.15.2
